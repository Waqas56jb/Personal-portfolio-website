# Create React App reads these at BUILD time and inlines them into the bundle.
#   npm start  -> .env.development
#   npm run build / Vercel -> .env.production
# Both of those files are committed because the API URL is not a secret.
# Use `.env.local` for anything you do not want in git.

REACT_APP_API_URL=http://localhost:5000
